node_modules/

# secrets / local config (templates .env.example + cypress.env.example.json ARE tracked)
.env
.env.local
.env.*.local
cypress.env.json

# transient runner output
cypress/videos/
cypress/screenshots/
cypress/downloads/
test-results/
playwright-report/
playwright/.cache/
blob-report/
# allure-results/ IS committed so CI (which has Java) can generate the Allure
# HTML report without needing the app — see .github/workflows/pages.yml.

# Published, COMMITTED artifacts live under docs/ (screenshots + reports) — NOT ignored.
